#testing 


All outgoing transitions of nodes.（其实我觉得这句定义更适合 [[branch coverage]]).

教材中好像不太区分 decision 和 branch coverage，教材中有这么两句话很难理解：
- 100% decision coverage includes 100% branch and statement coverage.
- 100% branch coverage includes 100% decision coverage.

问 GPT 之后，它给了一个区分 decision 和 branch coverage 的例子：

```ts
if (A || B) {
    S1();
}
S2();
```

```
          ┌───────────┐
          │   Start   │
          └─────┬─────┘
                │
          ┌─────▼─────┐
          │     A     │   ← 条件1
          └──┬─────┬─┘
        T    │     │    F
             │     │
     ┌───────▼┐ ┌──▼───┐
     │  S1()  │ │  B   │   ← 条件2
     └────┬───┘ └─┬───┬┘
          │       │   │
          │     T │   │ F
          │       │   │
          │   ┌───▼┐  │
          │   │ S1 │  │
          │   └─┬──┘  │
          │       │   │
          └───────┴───┘
                  │
              ┌───▼───┐
              │  S2() │
              └───┬───┘
                  │
               ┌──▼──┐
               │ End │
               └─────┘

```

⚠️ **Decision Coverage 只要求“判定表达式结果”**：

- `(A || B) == true` ✔（用例1）
- `(A || B) == false` ✔（用例2）

👉 **Decision Coverage = 100%**


❌ **Branch Coverage（不满足）**:

来看分支是否全部走过：

| 分支          | 是否覆盖    |
| ----------- | ------- |
| A == true   | ✔       |
| A == false  | ✔       |
| B == false  | ✔       |
| ❌ B == true | **未覆盖** |

👉 **存在一条控制流边没走过**